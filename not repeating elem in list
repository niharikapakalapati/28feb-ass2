def firstNonReapeating(arr,n):
    mp={}
    for i in range(n):
        if arr[i] not in mp:
           mp[arr[i]]=0
        mp[arr[i]]+=1
     for x in mp:
         if (mp[x]==1):
             print(x,end=" ")
 arr = [1,2,1,3,2,4,2,5,4,6,5,7,6,4 ]
 n = len(arr)
 firstNonRepeating(arr,n)
